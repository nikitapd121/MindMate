<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>LaunchStack | Get Your MVP Stack</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Quicksand', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-900 text-white min-h-screen px-6 py-10">
  <%- include('../partials/nav', { user: user }) %>

  <div class="max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold text-center text-purple-400 mb-8">🚀 LaunchStack: MVP Tech Stack Recommender</h1>

    <!-- INPUTS -->
    <div class="space-y-6">

      <!-- IDEA -->
      <div>
        <label for="idea" class="block font-semibold text-lg mb-2">Describe Your Product Idea</label>
        <textarea id="idea" rows="4" class="w-full p-4 bg-gray-800 text-white rounded border border-gray-600" placeholder="e.g., A marketplace for freelancers."></textarea>
      </div>

      <!-- PRESETS -->
      <div>
        <label for="presetIdeas" class="block font-semibold mb-2">Or select a preset:</label>
        <select id="presetIdeas" class="w-full p-3 bg-gray-800 text-white rounded border border-gray-600">
          <option value="">-- Choose an idea --</option>
          <option value="Real-time messaging app for startups">Chat App</option>
          <option value="AI assistant for writing documentation">AI Doc Assistant</option>
          <option value="Tool to schedule and manage remote teams">Remote Work Tool</option>
        </select>
      </div>

      <!-- PRODUCT TYPE -->
      <div>
        <label for="type" class="block font-semibold mb-2">📦 Type of Product</label>
        <select id="type" class="w-full p-3 bg-gray-800 text-white rounded border border-gray-600">
          <option value="">-- Select type --</option>
          <option value="B2B SaaS">B2B SaaS</option>
          <option value="Marketplace">Marketplace</option>
          <option value="Consumer Tool">Consumer Tool</option>
          <option value="Internal Tool">Internal Tool</option>
        </select>
      </div>

      <!-- PREFERENCES -->
      <div>
        <span class="block font-semibold mb-2">🛠️ Tech Preferences</span>
        <div class="flex flex-wrap gap-4">
          <label><input type="checkbox" name="prefs" value="mobile-first" class="mr-2"> Mobile-first</label>
          <label><input type="checkbox" name="prefs" value="no-code" class="mr-2"> No-code</label>
          <label><input type="checkbox" name="prefs" value="open-source" class="mr-2"> Open-source</label>
          <label><input type="checkbox" name="prefs" value="ai-powered" class="mr-2"> AI-powered</label>
          <label><input type="checkbox" name="prefs" value="serverless" class="mr-2"> Serverless</label>
        </div>
      </div>

      <!-- INVESTMENT -->
      <div>
        <label for="budget" class="block font-semibold mb-2">💰 Estimated Budget (in ₹ INR)</label>
        <input type="number" id="budget" class="w-full p-3 bg-gray-800 text-white rounded border border-gray-600" placeholder="e.g., 50000">
      </div>

      <!-- TEAM SIZE -->
      <div>
        <label for="team" class="block font-semibold mb-2">👥 Team Availability</label>
        <select id="team" class="w-full p-3 bg-gray-800 text-white rounded border border-gray-600">
          <option value="solo">Just Me</option>
          <option value="small">Small Team (2-5)</option>
          <option value="agency">Agency / External Team</option>
        </select>
      </div>

      <!-- PERSONAL NOTES -->
      <div>
        <label for="notes" class="block font-semibold mb-2">📝 Additional Notes</label>
        <textarea id="notes" rows="3" class="w-full p-3 bg-gray-800 text-white border border-gray-600 rounded" placeholder="Goals, deadline, tech constraints, etc."></textarea>
      </div>

      <!-- SUGGEST BUTTON -->
      <div class="text-center">
        <button id="suggestBtn" class="mt-6 bg-blue-600 hover:bg-blue-700 px-6 py-3 rounded font-semibold">
          💡 Suggest Stack
        </button>
      </div>
    </div>

    <!-- RESULT (STATIC FOR NOW) -->
    <div id="resultSection" class="mt-10 bg-gray-800 p-6 rounded-lg border border-gray-700">
      <h2 class="text-xl font-bold text-green-400 mb-4">✅ Suggested Stack (UI Only)</h2>
      <div class="space-y-2 text-sm text-gray-200 whitespace-pre-wrap">
        🧱 Frontend: React + Tailwind CSS<br>
        ⚙️ Backend: Node.js + Express<br>
        🔐 Auth: Firebase Auth<br>
        🗃️ Database: MongoDB Atlas<br>
        🚀 Deployment: Vercel or Render<br>
        🎨 UI Tools: Figma, Shadcn UI<br>
        🧹 Extras: Postman, GitHub, Notion
      </div>
    </div>

    <!-- FEEDBACK -->
    <div class="text-center mt-6">
      <a href="/contact" class="text-blue-400 underline text-sm hover:text-blue-300">Want better suggestions? Give feedback →</a>
    </div>
  </div>
</body>
</html>
